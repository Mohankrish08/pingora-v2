fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1

# Database
supabase==2.4.6

# JWT (RSA sign/verify)
python-jose[cryptography]==3.3.0

# Password hashing
passlib[bcrypt]==1.7.4

# Redis (OTP, refresh token store, rate limiting)
redis[hiredis]==5.0.4

# OTP generation
pyotp==2.9.0

# Email validation (required by pydantic's EmailStr)
email-validator==2.1.1

# dependencies
-e ../../common-helpers